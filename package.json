{
  "name": "rest-api-nodejs",
  "version": "1.0.0",
  "description": "A simple rest api server for debuging purposes. So we can try all methods in other app and get a response.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Samuel Parente",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.3"
  }
}
